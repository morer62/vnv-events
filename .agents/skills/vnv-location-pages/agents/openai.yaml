interface:
  display_name: "VNV Location Pages"
  short_description: "Build approved VNV city-location page batches"
  default_prompt: "Use $vnv-location-pages to propose or expand one VNV service into an approved 62-city location-page cycle."
policy:
  allow_implicit_invocation: true
