<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="author" content="VNV Events" />
    <title>{% block page_title %}VNV Events Dashboard{% endblock %}</title>

    <!-- Google Tag Manager -->
      <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
      new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
      j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
      'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
      })(window,document,'script','dataLayer','GTM-NCK9W86W');</script>
    <!-- End Google Tag Manager -->

    <link rel="shortcut icon" href="{{ asset_for('template/img/icons/icon-48x48.png') }}" />
    <link href="{{ asset_for('template/css/app.css') }}" rel="stylesheet" />
    <link href="{{ asset_for('assets/css/vnv-panel.css') }}" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.gstatic.com" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet" />
    <style>
        .sidebar [data-bs-toggle=collapse]:after {
            border: solid;
            border-width: 0 .075rem .075rem 0;
            content: " ";
            display: inline-block;
            padding: 2px;
            position: absolute;
            right: 1.5rem;
            top: 1.2rem;
            transform: rotate(45deg);
            transition: all .2s ease-out;
        }

        .sidebar [aria-expanded=true]:after, .sidebar [data-bs-toggle=collapse]:not(.collapsed):after {
            top: 1.4rem;
            transform: rotate(-135deg);
        }

        .sidebar-dropdown .sidebar-link:before {
            content: "â†’";
            display: inline-block;
            left: -14px;
            position: relative;
            transform: translateX(0);
            transition: all .1s ease;
        }

        .w-fit {
            width: fit-content;
        }

        .vnv-events-sidebar {
            --vnv-side-ink: #ecfffa;
            --vnv-side-muted: rgba(236, 255, 250, .62);
            --vnv-side-green: #49f2c2;
            --vnv-side-teal: #10b9a5;
            --vnv-side-deep: #15191d;
            --vnv-side-panel: rgba(255, 255, 255, .055);
            background:
                linear-gradient(180deg, #24282e 0%, #1c2025 46%, #121519 100%) !important;
            border-right: 1px solid rgba(73, 242, 194, .12);
            box-shadow: inset -1px 0 0 rgba(255,255,255,.045), 18px 0 44px rgba(0, 0, 0, .22);
        }

        .vnv-events-sidebar .sidebar-content {
            background: transparent !important;
            position: relative;
        }

        .vnv-events-sidebar .sidebar-content:before {
            content: "";
            position: absolute;
            inset: 0;
            pointer-events: none;
            background:
                linear-gradient(90deg, rgba(255,255,255,.032), transparent 38%),
                repeating-linear-gradient(135deg, rgba(255,255,255,.018) 0 1px, transparent 1px 18px);
            opacity: .62;
        }

        .vnv-events-sidebar .sidebar-brand {
            position: relative;
            z-index: 1;
            margin: 14px 14px 10px;
            padding: 18px 16px;
            border: 1px solid rgba(73, 242, 194, .18);
            border-radius: 22px;
            background:
                radial-gradient(circle at top right, rgba(73, 242, 194, .20), transparent 42%),
                linear-gradient(135deg, rgba(255,255,255,.10), rgba(255,255,255,.035));
            color: #fff !important;
            font-weight: 950;
            letter-spacing: -.02em;
            box-shadow: 0 18px 36px rgba(0,0,0,.18);
        }

        .vnv-events-sidebar .sidebar-brand span:before {
            content: "VNV Events";
            display: block;
            margin-bottom: 4px;
            color: var(--vnv-side-green);
            font-size: .72rem;
            font-weight: 950;
            letter-spacing: .16em;
            text-transform: uppercase;
        }

        .vnv-events-sidebar .sidebar-nav {
            position: relative;
            z-index: 1;
            padding: 4px 10px 26px;
        }

        .vnv-events-sidebar .sidebar-header {
            margin: 20px 8px 8px;
            padding: 0;
            color: var(--vnv-side-green) !important;
            font-size: .68rem;
            font-weight: 950;
            letter-spacing: .16em;
            text-transform: uppercase;
            opacity: 1;
        }

        .vnv-events-sidebar .sidebar-header:first-child {
            color: rgba(236,255,250,.45) !important;
        }

        .vnv-events-sidebar .sidebar-item {
            margin: 3px 0;
        }

        .vnv-events-sidebar .sidebar-link {
            position: relative;
            min-height: 46px;
            margin: 0;
            padding: 10px 12px;
            border: 1px solid transparent;
            border-radius: 16px;
            color: var(--vnv-side-muted) !important;
            background: transparent !important;
            font-weight: 780;
            transition: transform .16s ease, background .16s ease, border-color .16s ease, color .16s ease, box-shadow .16s ease;
        }

        .vnv-events-sidebar .sidebar-link i,
        .vnv-events-sidebar .sidebar-link svg {
            width: 18px;
            height: 18px;
            color: var(--vnv-side-green) !important;
            filter: drop-shadow(0 0 10px rgba(73, 242, 194, .22));
        }

        .vnv-events-sidebar .sidebar-link:before {
            content: "";
            width: 30px;
            height: 30px;
            position: absolute;
            left: 7px;
            top: 50%;
            transform: translateY(-50%) scale(.92);
            border-radius: 12px;
            background: rgba(73, 242, 194, .075);
            opacity: 0;
            transition: opacity .16s ease, transform .16s ease;
        }

        .vnv-events-sidebar .sidebar-link:hover,
        .vnv-events-sidebar .sidebar-item.active > .sidebar-link,
        .vnv-events-sidebar .sidebar-link[aria-expanded="true"] {
            color: var(--vnv-side-ink) !important;
            border-color: rgba(73, 242, 194, .20);
            background:
                linear-gradient(135deg, rgba(73, 242, 194, .16), rgba(255,255,255,.065)) !important;
            box-shadow: inset 0 1px 0 rgba(255,255,255,.08), 0 12px 26px rgba(0,0,0,.16);
            transform: translateX(2px);
        }

        .vnv-events-sidebar .sidebar-link:hover:before,
        .vnv-events-sidebar .sidebar-item.active > .sidebar-link:before,
        .vnv-events-sidebar .sidebar-link[aria-expanded="true"]:before {
            opacity: 1;
            transform: translateY(-50%) scale(1);
        }

        .vnv-events-sidebar .sidebar-link span {
            position: relative;
            z-index: 1;
            color: inherit !important;
        }

        .vnv-events-sidebar .sidebar-dropdown {
            margin: 4px 0 10px 18px;
            padding-left: 8px;
            border-left: 1px solid rgba(73, 242, 194, .14);
        }

        .vnv-events-sidebar .sidebar-dropdown .sidebar-link {
            min-height: 38px;
            border-radius: 13px;
            color: rgba(236,255,250,.56) !important;
            font-size: .88rem;
        }

        .vnv-events-sidebar .sidebar-dropdown .sidebar-link:before {
            display: none;
        }

        .vnv-events-sidebar .simplebar-scrollbar:before {
            background: rgba(73, 242, 194, .42);
        }

        @media (max-width: 767.98px) {
            .vnv-events-sidebar .sidebar-brand {
                margin: 12px;
            }

            .vnv-events-sidebar .sidebar-link {
                min-height: 48px;
            }
        }

        .floating-whatsapp{
            position:fixed;
            right:20px;
            bottom:20px;
            z-index:0;
            display:inline-flex;
            align-items:center;
            gap:.65rem;
            padding:.9rem 1.1rem;
            border-radius:999px;
            background:#25D366;
            color:#fff !important;
            text-decoration:none;
            font-weight:800;
            box-shadow:0 14px 30px rgba(0,0,0,.18);
            transition:transform .2s ease, box-shadow .2s ease;
        }

        .floating-whatsapp i{
            font-size:1.3rem;
        }

        .floating-whatsapp:hover{
            transform:translateY(-2px);
            box-shadow:0 18px 36px rgba(0,0,0,.22);
        }

        @media (max-width: 768px){
            .floating-whatsapp{
                right:14px;
                bottom:14px;
                width:66px;
                height:66px;
                justify-content:center;
                border-radius:50%;
                padding:0;
            }

            .floating-whatsapp span{
                display:none;
            }

            .floating-whatsapp i{
                font-size:2rem;
            }
        }
    </style>
    {% block style %}
    {% endblock %}


</head>

<body>

    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-NCK9W86W"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->


    <div class="wrapper">
        {% include 'templates/layout/sidebarLayout.html.twig' %}

        <div class="main">
            {% include 'templates/layout/headerLayout.html.twig' %}

            <main class="content">
                <div class="container-fluid p-0" id="content">
                    {% block body %}
                    {% endblock %}
                </div>
            </main>

            {% include 'templates/layout/footerLayout.html.twig' %}
        </div>
    </div>

    {% block aside %}
    {% endblock %}
    {% include 'templates/components/toast-component.twig' %}

    <script src="{{ asset_for('template/js/app.js') }}"></script>
    <script src="{{ asset_for('assets/js/luxon.min.js') }}"></script>
    <script>

    </script>
    <script>

        document.addEventListener('DOMContentLoaded', () => {
            {% if alertMessage is not null %}

                {% if isMobileApp %}
                    if (window.bootstrap) {
                        new bootstrap.Toast(document.getElementById('alertToast')).show();
                    }
                {% else %}
                    setTimeout(() => {
                        alert('{{ alertMessage.message }}')
                    }, 1)
                {% endif %}
            {% endif %}

            document.querySelectorAll("[data-phone]").forEach(elm => {
                elm.addEventListener("input", function (e) {
                    let value = e.target.value.replace(/\D/g, ""); // Remove non-numeric characters

                    if (value.startsWith("1")) {
                        value = "+" + value; // Ensure +1 is kept
                    } else if (value.length > 0) {
                        value = "+1" + value; // Add +1 if not present
                    }

                    if (value.length > 2 && value.length <= 5) {
                        value = `${value.slice(0, 2)} (${value.slice(2)}`;
                    } else if (value.length > 5 && value.length <= 8) {
                        value = `${value.slice(0, 2)} (${value.slice(2, 5)}) ${value.slice(5)}`;
                    } else if (value.length > 8) {
                        value = `${value.slice(0, 2)} (${value.slice(2, 5)}) ${value.slice(5, 8)}-${value.slice(8, 12)}`;
                    }

                    e.target.value = value;
                })
            })

            document.querySelectorAll("[form-delete]").forEach(elm => {
                elm.addEventListener("submit",function (e) {
                    e.preventDefault()
                    const isConfirmed = confirm("Are you sure you want to delete this item?")
                    if (isConfirmed) {
                        this.submit()
                    }
                })
            })
        })

        document.addEventListener('DOMContentLoaded', function () {
            const tooltipTriggerList = document.querySelectorAll('[data-bs-toggle="tooltip"]')

            for (const tooltipTriggerEl of tooltipTriggerList) {
                new bootstrap.Tooltip(tooltipTriggerEl)
            }
        })

        document.addEventListener('DOMContentLoaded', function () {
            const { DateTime } = luxon;
            const localTimes = document.querySelectorAll('[data-local-time]')

            localTimes.forEach(elm => {

                let utcText = elm.innerText
                let utcDateTime = DateTime.fromFormat(utcText, "yyyy-MM-dd HH:mm:ss", { zone: "utc" });
                const local = utcDateTime.setZone(DateTime.local().zoneName);
                elm.innerText = local.toFormat("yyyy-MM-dd HH:mm:ss")
            })
        })

    </script>
    <script src="https://maps.googleapis.com/maps/api/js?key={{ env.GOOGLE_KEY }}&loading=async&libraries=places&callback=initMap"></script>
    <script src="{{ asset_for('assets/js/googleMaps.js') }}" ></script>
    <script src="{{ asset_for('assets/js/vnv-async-forms.js') }}"></script>

    {% block scripts %}
    {% endblock %}

    {% if show_whatsapp is defined and show_whatsapp %}
        <a href="https://wa.me/13053761210"
        class="floating-whatsapp"
        target="_blank"
        rel="noopener noreferrer">
            <i class="fa-brands fa-whatsapp"></i>
            <span>Chat with us</span>
        </a>
    {% endif %}
</body>
</html>


