{% extends 'templates/base.admin.twig' %}
{% import 'templates/macros.twig' as macros %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h2 class="h4 mb-0">
                        <i data-feather="edit"></i> Create New Forum Topic
                    </h2>
                </div>

                <div class="card-body p-4">
                    <form method="POST" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" value="{{ get_csrf() }}">
                        <!-- Category -->
                        <div class="mb-4">
                            <label for="category_id" class="form-label fw-bold">
                                Category <span class="text-danger">*</span>
                            </label>
                            <select name="category_id" id="category_id" class="form-select" required>
                                <option value="">Select a category...</option>
                                {% for category in categories %}
                                <option value="{{ category.id }}" {% if selectedCategory == category.id %}selected{% endif %}>
                                    {{ category.icon|default('📁') }} {{ category.name }}
                                </option>
                                {% endfor %}
                            </select>
                        </div>

                        <!-- Title -->
                        <div class="mb-4">
                            <label for="title" class="form-label fw-bold">
                                Title <span class="text-danger">*</span>
                            </label>
                            <input type="text" name="title" id="title" class="form-control" 
                                   placeholder="Enter a descriptive title for your topic" 
                                   maxlength="500" required>
                            <div class="form-text">Make it clear and concise (max 500 characters)</div>
                        </div>

                        <!-- Content -->
                        <div class="mb-4">
                            <label for="content" class="form-label fw-bold">
                                Content <span class="text-danger">*</span>
                            </label>
                            <textarea name="content" id="content" class="form-control" 
                                      rows="12" placeholder="Write your post content here..." required></textarea>
                            <div class="form-text">Share your thoughts, questions, or insights</div>
                        </div>

                        <div class="row">
                            <div class="col-md-7 mb-4">
                                <label for="slug" class="form-label fw-bold">Public URL Slug</label>
                                <input type="text" name="slug" id="slug" class="form-control" placeholder="best-wedding-venues-in-miami">
                                <div class="form-text">Leave blank to generate it from the title.</div>
                            </div>
                            <div class="col-md-5 mb-4">
                                <label for="status" class="form-label fw-bold">Status</label>
                                <select name="status" id="status" class="form-select">
                                    <option value="PUBLISHED">Published</option>
                                    <option value="DRAFT">Draft</option>
                                    <option value="PAUSED">Paused</option>
                                    <option value="ARCHIVED">Archived</option>
                                </select>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label for="excerpt" class="form-label fw-bold">Excerpt</label>
                            <textarea name="excerpt" id="excerpt" class="form-control" rows="3" placeholder="Short public summary for listings and SEO."></textarea>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label for="seo_title" class="form-label fw-bold">SEO Title</label>
                                <input type="text" name="seo_title" id="seo_title" class="form-control" maxlength="255">
                            </div>
                            <div class="col-md-6 mb-4">
                                <label for="seo_description" class="form-label fw-bold">SEO Description</label>
                                <input type="text" name="seo_description" id="seo_description" class="form-control" maxlength="255">
                            </div>
                        </div>

                        <!-- Attachments -->
                        <div class="mb-4">
                            <label for="attachments" class="form-label fw-bold">
                                📎 Attachments (Optional)
                            </label>
                            <input type="file" name="attachments[]" id="attachments" 
                                   class="form-control" multiple accept="image/*,.pdf">
                            <div class="form-text">
                                You can upload multiple images or PDFs (max 5MB each)
                            </div>
                            <div id="preview" class="mt-3 row g-2"></div>
                        </div>

                        <!-- Options -->
                        <div class="mb-4">
                            <div class="form-check">
                                <input type="checkbox" name="is_pinned" id="is_pinned" class="form-check-input" value="1">
                                <label for="is_pinned" class="form-check-label">
                                    📌 Pin this topic (it will appear at the top of the category)
                                </label>
                            </div>
                            <div class="form-check mt-2">
                                <input type="checkbox" name="allow_replies" id="allow_replies" class="form-check-input" value="1" checked>
                                <label for="allow_replies" class="form-check-label">
                                    Allow Level 5 users to reply
                                </label>
                            </div>
                        </div>

                        <!-- Actions -->
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('panel/forum') }}" class="btn btn-outline-secondary">
                                <i data-feather="x"></i> Cancel
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i data-feather="send"></i> Publish Topic
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Guidelines -->
            <div class="card border-0 shadow-sm mt-4">
                <div class="card-body">
                    <h5 class="fw-bold mb-3">💡 Community Guidelines</h5>
                    <ul class="mb-0">
                        <li>Be respectful and courteous to all members</li>
                        <li>Stay on topic and post in the appropriate category</li>
                        <li>Use clear and descriptive titles</li>
                        <li>Search before posting to avoid duplicates</li>
                        <li>Do not spam or post promotional content</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
// Preview uploaded images
document.getElementById('attachments').addEventListener('change', function(e) {
    const preview = document.getElementById('preview');
    preview.innerHTML = '';
    
    const files = e.target.files;
    for (let i = 0; i < files.length; i++) {
        const file = files[i];
        
        if (file.type.startsWith('image/')) {
            const reader = new FileReader();
            reader.onload = function(e) {
                const col = document.createElement('div');
                col.className = 'col-md-3';
                col.innerHTML = `
                    <div class="position-relative">
                        <img src="${e.target.result}" class="img-fluid rounded" alt="${file.name}">
                        <div class="position-absolute top-0 end-0 p-1">
                            <span class="badge bg-dark">${(file.size / 1024).toFixed(0)}KB</span>
                        </div>
                    </div>
                `;
                preview.appendChild(col);
            };
            reader.readAsDataURL(file);
        } else {
            const col = document.createElement('div');
            col.className = 'col-md-12';
            col.innerHTML = `
                <div class="alert alert-secondary py-2 px-3 mb-2">
                    <i data-feather="file"></i> ${file.name} (${(file.size / 1024).toFixed(0)}KB)
                </div>
            `;
            preview.appendChild(col);
            feather.replace();
        }
    }
});
</script>
{% endblock %}





